#!/bin/sh

selection="$(fd . "$MPV_PLAYLISTS_DIR" -d 1 |sed 's@'"$MPV_PLAYLISTS_DIR"\/'@@' |fzf)"

if [ -z "$selection" ]; then
  echo "No selection"
  exit 0
fi

mpv --shuffle --playlist="$MPV_PLAYLISTS_DIR/$selection"
